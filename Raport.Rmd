---
title: "README"
author: "127310"
output: rmarkdown::github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
```{r}
library(reshape2)
library(ggplot2)
```

```{r, echo=FALSE}

multiplot <- function(..., plotlist=NULL, file, cols=1, layout=NULL) {
  library(grid)

  # Make a list from the ... arguments and plotlist
  plots <- c(list(...), plotlist)

  numPlots = length(plots)

  # If layout is NULL, then use 'cols' to determine layout
  if (is.null(layout)) {
    # Make the panel
    # ncol: Number of columns of plots
    # nrow: Number of rows needed, calculated from # of cols
    layout <- matrix(seq(1, cols * ceiling(numPlots/cols)),
                    ncol = cols, nrow = ceiling(numPlots/cols))
  }

 if (numPlots==1) {
    print(plots[[1]])

  } else {
    # Set up the page
    grid.newpage()
    pushViewport(viewport(layout = grid.layout(nrow(layout), ncol(layout))))

    # Make each plot, in the correct location
    for (i in 1:numPlots) {
      # Get the i,j matrix positions of the regions that contain this subplot
      matchidx <- as.data.frame(which(layout == i, arr.ind = TRUE))

      print(plots[[i]], vp = viewport(layout.pos.row = matchidx$row,
                                      layout.pos.col = matchidx$col))
    }
  }
}
```



1. Kod wyliczający wykorzystane biblioteki.
Biblioteki:
```{r, echo=FALSE}
(.packages())
```
3.Kod pozwalający wczytać dane z pliku.
4.Kod przetwarzający brakujące dane.
Wczytanie danych i oczyszczanie ich
```{r}
sledzie <- read.csv("sledzie.csv",header=TRUE,na.strings = "?")
sledzie_no_na <- na.omit(sledzie)
```
5.Sekcję podsumowującą rozmiar zbioru i podstawowe statystyki.
Podsumowanie:
```{r, echo=FALSE}
summary(sledzie)
```
6.Szczegółową analizę wartości atrybutów (np. poprzez prezentację rozkładów wartości).
```{r, echo=FALSE}
p1 <- ggplot(sledzie,aes(x=length)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p2 <- ggplot(sledzie,aes(x=cfin1)) + geom_histogram(color="darkblue",fill="lightblue",stat="count") 
p3 <- ggplot(sledzie,aes(x=cfin2)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p4 <- ggplot(sledzie,aes(x=chel1)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p5 <- ggplot(sledzie,aes(x=chel2)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p6 <- ggplot(sledzie,aes(x=lcop1)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p7 <- ggplot(sledzie,aes(x=lcop2)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p8 <- ggplot(sledzie,aes(x=fbar)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p9 <- ggplot(sledzie,aes(x=recr)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p10 <- ggplot(sledzie,aes(x=cumf)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p11 <- ggplot(sledzie,aes(x=totaln)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p12 <- ggplot(sledzie,aes(x=sst)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p13 <- ggplot(sledzie,aes(sal)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p14 <- ggplot(sledzie,aes(x=xmonth)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
p15 <- ggplot(sledzie,aes(x=nao)) + geom_histogram(color="darkblue",fill="lightblue",stat="count")
multiplot(p1,p2,p3,p4,p5,p6,p7,p8,p9,p10,p11,p12,p13,p14,p15,cols=3)
```
7.Sekcję sprawdzającą korelacje między zmiennymi; sekcja ta powinna zawierać jakąś formę graficznej prezentacji korelacji.

8.Interaktywny wykres lub animację prezentującą zmianę rozmiaru śledzi w czasie.

9.Sekcję próbującą stworzyć regresor przewidujący rozmiar śledzia (w tej sekcji należy wykorzystać wiedzę z pozostałych punktów oraz wykonać dodatkowe czynności, które mogą poprawić trafność predykcji); dobór parametrów modelu oraz oszacowanie jego skuteczności powinny zostać wykonane za pomocą techniki podziału zbioru na dane uczące, walidujące i testowe; trafność regresji powinna zostać oszacowana na podstawie miar R2 i RMSE.

10.Analizę ważności atrybutów najlepszego znalezionego modelu regresji. Analiza ważności atrybutów powinna stanowić próbę odpowiedzi na pytanie: co sprawia, że rozmiar śledzi zaczął w pewnym momencie maleć.
